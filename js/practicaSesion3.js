    // ! Identificar el numero mayor de dos usando el operador ternary

    // let numA = parseInt(prompt('Ingresa el primer numero'));
    // let numB = parseInt(prompt('Ingresa el segundo numero'));

    // let numeroMayor = numA == numB ? 'Los 2 son iguales' : (numA > numB ? 'El primer numero es mayor' : 'El segundo numero es mayor');
    // console.log(numeroMayor);

    // ! Identificar el numero mayor de tres usando el operador ternary

    // let numA = parseInt(prompt('Ingresa el primer numero'));
    // let numB = parseInt(prompt('Ingresa el segundo numero'));
    // let numC = parseInt(prompt('Ingresa el tercer numero'));

    // let numeroMayor = (numA > numB && numA > numC) ? 'El mayor es el primer numero' : (numB > numC ? 'El mayor es el segundo numero' : ((numA == numB || numA == numC || numB == numC) ? 'Hay al menos 2 numeros iguales'  : 'El mayor es el tercer numero' ));
    // console.log(numeroMayor);

    // ! Pedir 2 numeros al usuario
        // Si a mayor que  b dividir a entre b
        // Si a menor que  b sumar a más b
        // Si a  es igual b multiplicar ambos números
        // Imprimir el resultado en consola

    // let numA = parseInt(prompt('Dame un numero'));
    // let numB = parseInt(prompt('Dame otro numero'));

    // let res = numA == numB ? (numA * numB) : (numA > numB ? (numA / numB) : (numA + numB));
    // console.log(res); 


