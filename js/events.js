// ! Eventos
    //Estos suceden sobre los elementos de nuestro DOM y podemos capturar y reaccionar a ellos, manipularlos a nuestro favor

// ! Tipos de eventos
    // Eventos automáticos
    // Eventos generados por usuarios

// ? Estilos en línea => Online events

function imgGrande(x) {
    x.style.height = "64px";
    x.style.width = "64px";
  }
  
  function imgNormal(x) {
    x.style.height = "32px";
    x.style.width = "32px";
  }