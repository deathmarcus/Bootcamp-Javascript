// ? Metodos de strings (Ninguno de los metodos de String modifica el string original)
    // length --> (Propiedad) <-- Nos devuelve la longitud de un string
    // String.length

    // charAt(<index al cual acceder>) <-- Devuelve el caracter del index indicado
    // String.charAt(0)

    // toUpperCase() <-- Nos retorna un string en Mayuscula

    // toLowerCase() <-- Nos retorna un string en Minuscula

    // subString(<posicion inicial> , ?<posicion final>) <-- Retorna la porcion de string indicada
    // String.substring(0)    

    // slice(<posicion inicial> , ?<posicion final>) <-- Retorna la porcion de string indicada | Acepta numeros negativos (los lee del final al inicio)
    // String.slice(-1)

    // indexOf(<valor a buscar>, ?<posicion inicial>) <-- Regresa el indice de la primera coincidencia del caracter indicado | En caso de que no lo encuentre regresa -1 
    // String.indexOf('o', 2)

    // replace(<valor a reemplazar> , <nuevo valor>) <-- Reemplaza el valor de un caracter
    // String.replace('o' , 'x')

    // replaceAll(<valor a reemplazar> . <nuevo valor>) <-- Reemplaza el valor de todos los caracteres que coincidan
    // String.replaceAll('o' , 'x')

    // trim() <-- Limpia el string de espacios vacios al principio y al final
    // String.trim()

    

